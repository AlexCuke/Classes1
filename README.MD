Задача 1. Класс «Кот»
Сделайте класс Cat, который хранит имя и возраст кота.​
Требования:​
В конструкторе __init__ принимать name и age и сохранять их в self.name и self.age.​
Сделать метод meow(self), который печатает строку вида "Мяу! Я кот Вася, мне 3 года" (подставляя реальные значения).​
Создать два объекта Cat с разными именами и возрастом и вызвать для каждого метод meow().​
Задача 2. Класс «Круг»
Сделайте класс Circle, который хранит радиус круга и может считать его площадь.​
Требования:​
В конструкторе принимать radius и сохранять его в self.radius.​
Сделать метод area(self), который возвращает площадь круга по формуле S=3.14⋅r2S=3.14⋅r2.​
В основной части программы создать круг с радиусом 5 и вывести его площадь на экран.​
Задача 3. Класс «Счётчик»
Сделайте класс Counter, который считает, сколько раз его «увеличивали».​
Требования:​
В конструкторе создать атрибут self.value и поставить туда 0.​
Сделать метод inc(self), который увеличивает self.value на 1.​
Сделать метод reset(self), который сбрасывает self.value в 0.​
В основной части: создать объект Counter, три раза вызвать inc(), вывести значение, затем вызвать reset() и снова вывести значение.​
Задача 4. Класс «Ученик»
Сделайте класс Student, который хранит имя ученика и его оценки.​
Требования:​
В конструкторе принимать name, сохранять в self.name и создавать пустой список self.grades.​
Метод add_grade(self, grade) добавляет оценку в список.​
Метод average(self) возвращает среднюю оценку ученика (сумма / количество, если оценок нет — вернуть 0).​
В основной части: создать ученика, добавить несколько оценок и вывести его имя и средний балл.​
Задача 5. Наследование: «Животное» и «Собака»
Сделайте базовый класс Animal и класс Dog, который от него наследуется.​
Требования:​
В классе Animal сделать метод speak(self), который просто печатает "Какое-то животное издаёт звук".​
Отнаследовать класс Dog(Animal).​
В Dog переопределить метод speak(self), чтобы он печатал "Гав-гав!".​
Создать объекты Animal и Dog и вызвать для каждого speak().​
Задача 6. Магический метод __str__
Сделайте класс Point, который представляет точку на плоскости.​
Требования:​
В конструкторе принимать x и y и сохранять как self.x, self.y.​
Определить метод __str__(self), который возвращает строку вида "Point(x=1, y=2)".​
Создать пару точек и вывести их через print(point), чтобы на экране были красивые строки.​
Задача 7. Класс «Прямоугольник»
Сделайте класс Rectangle, который хранит ширину и высоту.
Требования:
В __init__(self, width, height) сохранить аргументы в self.width и self.height.
Метод area(self) возвращает площадь прямоугольника.
Метод perimeter(self) возвращает периметр прямоугольника.
В основной части программы создать пару прямоугольников и вывести их площадь и периметр.
Задача 8. Класс «Банковский счёт»
Сделайте класс BankAccount для простого банковского счёта.
Требования:
В __init__(self, owner, balance=0) сохранить имя владельца и начальный баланс.
Метод deposit(self, amount) увеличивает баланс на amount.
Метод withdraw(self, amount) уменьшает баланс на amount, но не даёт уйти в минус (если денег не хватает, ничего не менять и печатать сообщение).
Метод get_balance(self) возвращает текущий баланс.
В основной части: создать счёт, положить деньги, снять деньги, вывести баланс.
Задача 9. Класс «Таймер»
Сделайте класс Timer, который отсчитывает секунды до нуля.
Требования:
В __init__(self, seconds) сохранить количество секунд в self.seconds.
Метод tick(self) уменьшает self.seconds на 1, но не опускается ниже 0.
Метод is_finished(self) возвращает True, если self.seconds равно 0, иначе False.
В основной части: создать таймер на 5 секунд, несколько раз вызвать tick() и после каждого шага выводить оставшиеся секунды и is_finished().
Задача 10. Класс «Товар» и «Корзина»
Сделайте два класса: Item (товар) и Cart (корзина).
Требования для Item:
В __init__(self, name, price) сохранить название и цену товара.
Требования для Cart:
В __init__(self) создать пустой список self.items.
Метод add_item(self, item) добавляет объект Item в список.
Метод total(self) возвращает общую сумму цен всех товаров в корзине.
В основной части: создать несколько товаров, добавить их в корзину и вывести общую стоимость.
Задача 11. Класс «Температура»
Сделайте класс Temperature, который хранит температуру в градусах Цельсия.
Требования:
В __init__(self, celsius) сохранить значение в self.celsius.
Метод to_fahrenheit(self) возвращает температуру в Фаренгейтах по формуле F=C⋅9/5+32F=C⋅9/5+32.
Метод is_freezing(self) возвращает True, если температура меньше или равна 0, иначе False.
В основной части: создать несколько объектов с разной температурой и проверить, замерзает ли вода при такой температуре.
Задача 12. Класс «Вектор2D»
Сделайте класс Vector2D для вектора на плоскости.
Требования:
В __init__(self, x, y) сохранить координаты в self.x и self.y.
Метод add(self, other) возвращает новый объект Vector2D, являющийся суммой текущего вектора и other.
Метод length(self) возвращает длину вектора по формуле x2+y2x2+y2.
В основной части: создать два вектора, получить их сумму и вывести длину каждого.
Задача 13. Наследование: «Работник» и «Менеджер»
Сделайте базовый класс Employee и класс Manager, который от него наследуется.
Требования для Employee:
В __init__(self, name, salary) сохранить имя и зарплату.
Метод info(self) возвращает строку вида "Имя: ..., зарплата: ...".
Требования для Manager(Employee):
В __init__(self, name, salary, team_size) вызывать конструктор Employee и дополнительно сохранять размер команды.
Переопределить метод info(self), чтобы в строку добавлялось "размер команды: ...".
В основной части: создать Employee и Manager и вывести info() для каждого.
Задача 14. Класс с общим счётчиком объектов
Сделайте класс User, который считает, сколько объектов этого класса создано.
Требования:
Создать атрибут класса count = 0 (не в __init__, а прямо внутри класса).
В __init__(self, name) сохранять имя и каждый раз увеличивать User.count на 1.
Метод get_name(self) возвращает имя пользователя.
Статический или классовый метод get_count() возвращает, сколько пользователей уже создано.
В основной части: создать несколько пользователей и проверить, что счётчик показывает правильное число.
Задача 1. Склад товаров (композиция + классы)
Сделайте систему классов для простого склада.​
Требования:
Класс Product: атрибуты name, price, quantity.​
Класс Warehouse: хранит список продуктов self.products.​
Методы Warehouse:
add_product(product): добавить товар (если товар с таким именем уже есть — увеличить его quantity).​
remove_product(name, qty): уменьшить количество, если не хватает — выбросить ValueError.​
total_value(): вернуть общую стоимость всех товаров на складе.​
В основной части: создать склад, добавить несколько товаров, часть «продать», вывести итоговую стоимость.​
Задача 2. Библиотека книг (композиция + поиск)
Нужно смоделировать библиотеку.​
Требования:
Класс Book: title, author, year, is_available (по умолчанию True).​
Класс Library: хранит список книг.​
Методы Library:
add_book(book) — добавить книгу.​
find_by_author(author) — вернуть список книг этого автора.​
borrow(title) — найти книгу по названию, если доступна — пометить is_available = False, иначе выдать сообщение, что книгу уже взяли.​
return_book(title) — вернуть книгу, снова сделать is_available = True.​
В основной части: создать несколько книг, добавить в библиотеку, «выдать» и «вернуть» одну из них, вывести список доступных.​
Задача 3. Фигуры: площадь и периметр (наследование + полиморфизм)
Сделайте иерархию геометрических фигур.​
Требования:
Базовый класс Shape с абстрактными методами area() и perimeter() (можно просто raise NotImplementedError).​
Класс Rectangle(Shape) с атрибутами width, height и реализацией area и perimeter.​
Класс Circle(Shape) с атрибутом radius и методами area, perimeter (длина окружности).​
Класс Square(Rectangle) у которого в конструкторе только один параметр side, внутри вызывает super().__init__(side, side).​
В основной части: создать список фигур разных типов и в цикле вывести их тип, площадь и периметр, вызывая одни и те же методы area() и perimeter() (полиморфизм).​
Задача 4. Банк: клиенты и счета (наследование + классовые атрибуты)
Смоделируйте систему банковских счетов.​
Требования:
Класс Account:
Атрибут класса interest_rate (например, 0.03 для 3% годовых).​
В __init__: owner, balance.​
Методы deposit(amount), withdraw(amount) (не давать уйти в минус), add_interest() — прибавляет проценты к балансу.​
Класс SavingsAccount(Account) — накопительный счёт, у которого своя ставка, например interest_rate = 0.05.​
Класс CheckingAccount(Account) — расчетный счёт, комиссия за каждое снятие, например 1 у.е., учитывается в withdraw.​
В основной части: создать по одному счёту каждого типа, сделать несколько операций, начислить проценты и вывести итоговые балансы.​
Задача 5. Меню ресторана и заказ (композиция + магические методы)
Нужно сделать простую модель заказа в ресторане.​
Требования:
Класс MenuItem: name, price.​
Класс Order: хранит список позиций и реализует:
add_item(item, quantity) — добавить позицию в заказ.​
total() — сумма заказа.​
Магический метод __str__, который возвращает красивый чек со строками вида "Блюдо — кол-во — сумма" и итогом внизу.​
Дополнительно: перегрузить __len__, чтобы len(order) возвращал количество разных позиций в заказе.​
В основной части: создать несколько позиций меню, собрать заказ из них и вывести его.​
Задача 6. Система задач (ToDo) с приоритетами (инкапсуляция + сортировка)
Сделайте простой менеджер задач.​
Требования:
Класс Task:
Приватные атрибуты __title, __priority (число, где 1 — высокий приоритет, 3 — низкий).​
Свойства (@property) для чтения и изменения приоритета с проверкой, что он в диапазоне 1–3.​
Магический __str__, возвращающий строку "Задача: <title>, приоритет: <priority>".​
Класс TaskManager:
Хранит список задач.​
Метод add_task(task).​
Метод get_tasks_by_priority(), который возвращает задачи, отсортированные по приоритету (1 сначала).​
Метод remove_task(title), который удаляет задачу по названию.​
В основной части: создать несколько задач с разными приоритетами, добавить в менеджер, вывести отсортированный список, удалить одну задачу и снова вывести.​